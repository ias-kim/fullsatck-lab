// const { findByEmail } = require('../models/auth.model.js');
//
// exports.login = async (req, res) => {
//   const { email } = req.body;
//   const users = await findByEmail(email);
//
//   if (!users) {
//     res.status(401).json({ message: '존재하지 않는 이메일' });
//   }
// };


